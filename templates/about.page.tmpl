{{ template "base" .}}
{{ define "content" }}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>About page</h1>
                <p>Text About</p>
                <p>This came from {{ index .StringMap "test"}}</p>
                <p>{{ index .StringMap "name"}}</p>
            </div>
        </div>
    </div>
{{ end }}